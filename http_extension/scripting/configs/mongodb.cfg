"MongoDB Configuration"
{
	// MongoDB API Service Configuration
	"api_service"
	{
		// API service URL (where the Node.js service is running)
		"url"			"http://127.0.0.1:3300"

		// Connection timeout in milliseconds
		"timeout"		"30000"

		// Number of retry attempts for failed requests
		"retries"		"3"

		// Enable debug logging
		"debug"			"0"

		// API authentication (if your API service requires it)
		"api_key"		""

		// MongoDB connection settings (sent to API service)
		"mongodb"
		{
			// MongoDB connection URI (sent to API service for dynamic connections)
			"uri"			"mongodb://admin:password@37.114.54.74:27017/?authSource=admin"

			// Alternative: specify components separately
			"host"			"37.114.54.74"
			"port"			"27017"
			"username"		"admin"
			"password"		"password"
			"auth_database"		"admin"
			"use_ssl"		"0"
		}
	}
	
	// Default database settings
	"database"
	{
		// Default database name
		"name"			"gamedb"
		
		// Default collections
		"players_collection"	"players"
		"connections_collection" "connections"
		"stats_collection"	"statistics"
	}
	
	// Connection pooling settings
	"connection_pool"
	{
		// Maximum concurrent connections
		"max_connections"	"10"
		
		// Connection idle timeout (seconds)
		"idle_timeout"		"300"
	}
}
